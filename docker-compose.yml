version: "3"
services:
    nginx:
        image: andykuszyk/nginx:[version]
        ports: 
            - "80:80"
        networks:
            - net
        volumes:
            - /root/certs-data/:/data/letsencrypt/
networks:
    net:
